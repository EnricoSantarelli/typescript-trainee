"use strict";
// > => maior
// >= => maior ou igual
// < => menor
// <= => menor ou igual
// === igual
// !== diferente
// usado para testar em condições
// maior, maior ou igual
const idade = 20;
const maiorDeIdade = (minhaIdade >= 18); // true
// menor, menor ou igual
const menorDeIdade = (minhaIdade < 18); // false
// diferente, igual
const minhaAltura = 1.80;
const suaAltura = 1.90;
const somosIguais = (minhaAltura === suaAltura); // false
